# ingesta y datos estructurados
pandas>=2.2
openpyxl>=3.1
requests>=2.32

# RAG
langchain>=0.3
langchain-community>=0.3
langchain-text-splitters>=0.3
pypdf>=4.3
# loader por defecto para el chunker (ver ingestion/pdf_chunking.py) --
# decidido con evidencia sobre PyPDFLoader (0 vs 402 palabras rotas en
# el PDF de fosfatos, ~8x más rápido), ver CLAUDE.md.
pymupdf>=1.28
# torch CPU-only explícito (sesión 18-ago-2026, continuación 20) -- sin
# esto, `pip install torch` en un host Linux normal resuelve el build
# con CUDA por defecto (arrastra los paquetes nvidia-*-cu12, mucho más
# pesado en descarga/instalación/RAM que la reducción medida de ONNX
# int8 por sí sola no compensa, ver CLAUDE.md "Backend de embeddings:
# ONNX int8, no torch"). Fijado aquí para que el deploy real en la nube
# instale la versión ligera SIN depender de que alguien lo recuerde a
# mano en cada entorno nuevo. Versión (2.13.0) elegida por ser la que
# ya está validada en el venv de desarrollo (build CUDA) -- confirmado
# que download.pytorch.org/whl/cpu publica el mismo 2.13.0+cpu. El
# sufijo de versión local "+cpu" es lo que fuerza a pip a resolver
# desde este índice y no desde el build CUDA por defecto de PyPI.
# NOTA -- el venv de desarrollo LOCAL sigue con el build CUDA instalado
# a mano (útil para reindexados rápidos, ver CLAUDE.md) y no se
# reinstala solo por este pin; solo un `pip install -r requirements.txt`
# limpio (como el que corre el deploy) se lleva el build CPU-only.
--extra-index-url https://download.pytorch.org/whl/cpu
#torch==2.13.0+cpu
# extra [onnx] -- añade optimum + onnxruntime, necesarios para
# ingestion/embedding_model.py (backend ONNX + pesos int8, decisión de
# memoria del deploy, sesión 18-ago-2026) -- torch sigue siendo
# dependencia dura del paquete pase lo que pase, no se puede quitar de
# aquí (verificado desinstalándolo: rompe el import incluso con este
# extra), ver CLAUDE.md.
sentence-transformers[onnx]>=3.0
chromadb>=0.5

# orquestación
langgraph>=0.2

# LLM (DeepSeek, API compatible OpenAI)
openai>=1.40

# MCP -- servidor implementado con mcp.server.mcpserver.MCPServer, API
# que solo existe desde 2.0 (en 1.x era mcp.server.fastmcp.FastMCP,
# módulo distinto) -- pin >=1.0 permitía instalar una 1.x incompatible
# con src/efsa_rag/mcp/server.py, corregido tras verificar la versión
# real instalada (2.0.0) y su estructura de paquete.
mcp>=2.0

# UI
streamlit>=1.38

# Descarga de artefactos de deploy (xlsx + índice de Chroma) desde
# MEGA S4 en el arranque -- ver efsa_rag/deploy_assets.py y
# scripts/upload_deploy_assets.py. Los datos NUNCA van en git (ni
# siquiera vía Git LFS en un repo privado), por la licencia de los
# PDFs -- ver CLAUDE.md/PROGRESS.md, sesión 18-ago-2026 continuación 21.
boto3>=1.34

# Carga automática de .env (deploy_assets.py) -- credenciales MEGA S4 en
# desarrollo local sin depender de exportarlas a mano en cada terminal.
# Sin efecto en Streamlit Community Cloud (ahí llegan como variables de
# entorno reales vía "Secrets", no hay .env).
python-dotenv>=1.0

# dev
pytest>=8.0
